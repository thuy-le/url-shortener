import {Routing} from "../Routing";


export default class PingRouting implements Routing {

    public handlers() {
        return {
            getPing: (req: any) => {
                return "Hello, " + req.params.name;
            },
        };
    }

    public fillRoutes(router: any, handlers: any) {
        router.get("/ping/:name", handlers.getPing);
    }
}
